export { DatePicker } from "../DatePicker";

// Directory re-export to allow import paths like
// `components/shared/form/Datepicker` (no file extension and different casing)
// to resolve to the canonical `DatePicker` implementation without creating
// a same-directory filename that only differs in case.
